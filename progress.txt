## Codebase Patterns
- Main page is a single index.html with inline styles and one JS file (js/bitcoinprice.js)
- Price fetched from Coinbase API: https://api.coinbase.com/v2/prices/{COIN}-USD/spot
- Price display element id: btc-price-display, container id: btc-container
- `currentCoin` variable in js/bitcoinprice.js controls which coin is fetched/displayed
- Use `switchCoin(code)` to change coins - handles localStorage persistence + price refresh
- Main update function is `updatePrice()` - call it after changing `currentCoin` to refresh
- No build tools, linting, or tests - this is a plain HTML/JS project
- Search input: #coin-search (container div), #coin-input (text input), toggle via .visible class
- `showSearch()` and `hideSearch()` functions control search input visibility
- `startPolling()` clears and restarts the price update interval - use when switching coins
- `addToHistory(previousCoin)` handles coinHistory updates — dedup, exclude active, cap at 5, persist to localStorage
- Two places switch coins: `switchCoin()` and inline Enter handler — both call `addToHistory`
- #coin-error div displays validation errors inline below the input
- #coin-label is a clickable span showing current coin code (64px bold text)
- Price from API is `bodyObj.data.amount` (a string) — display it directly, no parsing needed
- `renderHistory()` rebuilds #coin-history tags from `coinHistory` array — called by `addToHistory()` and on page load
- History tags are `<button class="history-tag">` elements inside #coin-history div
---

## 2026-03-21 - US-001
- Removed `.split(".")[0]` from both `document.title` and `#btc-price-display` assignments
- Files changed: js/bitcoinprice.js
- **Learnings for future iterations:**
  - The price value from Coinbase API is already a formatted string — just prefix with `$`
  - No build/lint/test steps needed for this plain HTML/JS project
---

## 2026-03-21 - US-002
- Added `coinHistory` array persisted in localStorage as JSON
- Added `addToHistory(previousCoin)` function that deduplicates, excludes active coin, and caps at 5
- Called `addToHistory` from both `switchCoin()` and the search input Enter handler
- History is loaded from localStorage on page load
- Files changed: js/bitcoinprice.js
- **Learnings for future iterations:**
  - Two places switch coins: `switchCoin()` and the inline Enter handler in `coinInput` keydown listener — both need updating for coin-switch side effects
  - `addToHistory` takes the *previous* coin as argument since `currentCoin` is already updated when it runs
---

## 2026-03-21 - US-003
- Added #coin-history div below #btc-price-display in index.html
- Added CSS for .history-tag buttons (small, muted, rounded badges)
- Added `renderHistory()` function that creates clickable button tags from `coinHistory`
- `renderHistory()` is called from `addToHistory()` (on every change) and on page load
- Clicking a tag calls `switchCoin(coin)` + `startPolling()`
- Files changed: index.html, js/bitcoinprice.js
- **Learnings for future iterations:**
  - `renderHistory()` clears and rebuilds #coin-history innerHTML each time — simple but effective for small lists
  - History tags already handle click-to-switch via `switchCoin()`, which updates history and re-renders
---

## 2026-03-21 - US-004
- No code changes needed — all acceptance criteria were already implemented in US-003
- Click handler in `renderHistory()` calls `switchCoin(coin)` + `startPolling()`
- `switchCoin()` persists selectedCoin to localStorage and calls `addToHistory(previousCoin)`
- `addToHistory()` updates coinHistory (dedup, exclude active, cap at 5), persists to localStorage, and re-renders tags
- Files changed: prd.json (marked passes: true)
- **Learnings for future iterations:**
  - US-003 and US-004 were tightly coupled — implementing display with click handlers naturally satisfied both stories
  - When reviewing stories, check if prior work already covers the acceptance criteria before writing new code
---

